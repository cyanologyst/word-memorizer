<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="WordReviewReminder.MiniWidgetWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Word widget">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Border Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="14">
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="3">
                <TextBlock Text="Word Review" FontWeight="SemiBold" />
                <TextBlock x:Name="StatusText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="MetaText" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <Button Width="38" Height="34" Click="ReviewButton_Click">
                    <FontIcon Glyph="&#xE768;" />
                </Button>
                <Button Width="38" Height="34" Click="PauseButton_Click">
                    <FontIcon Glyph="&#xE769;" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
