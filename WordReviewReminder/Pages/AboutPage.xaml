<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WordReviewReminder.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="28,20,28,28" Spacing="18">
            <StackPanel>
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="About" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="A local Windows 11 vocabulary reminder for focused review" />
            </StackPanel>

            <Border Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel Spacing="10">
                    <TextBlock Text="Word Review Reminder" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock TextWrapping="WrapWholeWords" Text="The app ships with the TOEFL 550 March 2026 seed list extracted from the provided PDF. Additional databases can be imported from .wordlist.json files documented in docs/wordlist-json-format.md." />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Storage: %LOCALAPPDATA%\WordReviewReminder" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
