<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WordReviewReminder.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Padding="{StaticResource CompactPagePadding}" RowSpacing="14" Background="{StaticResource PremiumPageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Style="{StaticResource PremiumPageTitleStyle}" Text="Logs" />
                <TextBlock Style="{StaticResource PremiumPageSubtitleStyle}" Text="Recent review activity and exportable history" />
            </StackPanel>
            <ComboBox x:Name="FilterBox" Grid.Column="1" MinWidth="160" Header="Event type" SelectionChanged="FilterBox_SelectionChanged">
                <ComboBoxItem Content="All events" />
                <ComboBoxItem Content="Known" />
                <ComboBoxItem Content="Later" />
                <ComboBoxItem Content="Skipped" />
            </ComboBox>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource PremiumPanelStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="44" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Padding="18,0" ColumnSpacing="14" Background="{StaticResource PremiumSurfaceStrongBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="154" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="140" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Text="TIME" />
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Text="WORD / SOURCE" />
                    <TextBlock Grid.Column="2" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Text="RESULT" />
                </Grid>
                <ListView
                    x:Name="LogsView"
                    Grid.Row="1"
                    Padding="6,4,2,4"
                    HorizontalContentAlignment="Stretch"
                    ScrollViewer.VerticalScrollBarVisibility="Visible">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="MinHeight" Value="58" />
                            <Setter Property="Padding" Value="12,4" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="50" ColumnSpacing="14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="140" />
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center" Spacing="2">
                                    <TextBlock FontWeight="SemiBold" Text="{Binding Time}" />
                                    <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}" Text="{Binding Date}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                    <TextBlock FontWeight="SemiBold" Text="{Binding Term}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding WordListId}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <FontIcon Glyph="{Binding ActionGlyph}" FontSize="14" Foreground="{Binding ActionBrush}" />
                                    <TextBlock VerticalAlignment="Center" Text="{Binding Action}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Border x:Name="EmptyLogsPanel" Grid.Row="1" Margin="16" Style="{StaticResource EmptyStatePanelStyle}" Visibility="Collapsed">
                    <StackPanel Spacing="8" HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE823;" FontSize="24" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="EmptyLogsTitle" Style="{StaticResource EmptyStateTitleStyle}" Text="No review history yet" />
                        <TextBlock x:Name="EmptyLogsMessage" MaxWidth="420" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Complete a review and the result will appear here." TextAlignment="Center" TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <CommandBar Grid.Row="2" Background="Transparent" DefaultLabelPosition="Right" IsDynamicOverflowEnabled="False">
            <AppBarButton Icon="Save" Label="Export logs" AutomationProperties.Name="Export logs as JSON Lines" Click="ExportButton_Click" />
            <AppBarButton Icon="Refresh" Label="Refresh" AutomationProperties.Name="Refresh review history" Click="RefreshButton_Click" />
            <CommandBar.Content>
                <TextBlock x:Name="StatusText" AutomationProperties.LiveSetting="Polite" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </CommandBar.Content>
        </CommandBar>
    </Grid>
</Page>
