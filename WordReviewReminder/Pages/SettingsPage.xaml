<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WordReviewReminder.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <ScrollViewer>
        <StackPanel Padding="28,20,28,28" Spacing="20">
            <StackPanel>
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Settings" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Reminder timing and notification behavior" />
            </StackPanel>

            <Border Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel Spacing="16">
                    <TextBlock Text="Reminder behavior" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <Grid ColumnSpacing="16" RowSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock VerticalAlignment="Center" Text="Interval minutes" />
                        <NumberBox x:Name="IntervalBox" Grid.Column="1" Minimum="1" Maximum="180" SmallChange="1" LargeChange="5" />

                        <TextBlock Grid.Row="1" VerticalAlignment="Center" Text="Notification mode" />
                        <ComboBox x:Name="NotificationModeBox" Grid.Row="1" Grid.Column="1" MinWidth="220">
                            <ComboBoxItem Content="Popup" />
                            <ComboBoxItem Content="Toast" />
                            <ComboBoxItem Content="Both" />
                        </ComboBox>

                        <TextBlock Grid.Row="2" VerticalAlignment="Center" Text="Popup duration seconds" />
                        <NumberBox x:Name="PopupDurationBox" Grid.Row="2" Grid.Column="1" Minimum="5" Maximum="120" SmallChange="1" LargeChange="5" />

                        <TextBlock Grid.Row="3" VerticalAlignment="Center" Text="Word selection" />
                        <ComboBox x:Name="SelectionModeBox" Grid.Row="3" Grid.Column="1" MinWidth="220">
                            <ComboBoxItem Content="Due first" />
                            <ComboBoxItem Content="Random" />
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel Spacing="16">
                    <TextBlock Text="Quiet hours" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <ToggleSwitch x:Name="QuietHoursToggle" Header="Pause reminders during quiet hours" />
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="220" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="6">
                            <TextBlock Text="Start" />
                            <TimePicker x:Name="QuietStartPicker" ClockIdentifier="24HourClock" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Spacing="6">
                            <TextBlock Text="End" />
                            <TimePicker x:Name="QuietEndPicker" ClockIdentifier="24HourClock" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel Spacing="16">
                    <TextBlock Text="System" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <ToggleSwitch x:Name="StartWithWindowsToggle" Header="Start with Windows" />
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button Content="Save settings" Click="SaveButton_Click" />
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
